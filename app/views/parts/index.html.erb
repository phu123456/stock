<p id="notice"><%= notice %></p>
<%= render '/layouts/header' %>

<h2 align="center">อะไหล่</h2>


<div align="center">
  <%= form_tag parts_path, :method => 'get' do %>
    <p>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil %>
    </p>
  <% end %>
<div>

<table class="table table-striped" style="width:75%" align="center">
  <thead>
    <tr>
      <th class="ui-helper-center">Name</th>
      <th class="ui-helper-center">Quantity</th>
      <th class="ui-helper-center">ห้องเก็บ</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parts.each do |part| %>
      <tr>
        <td class="ui-helper-center"><%= part.name %></td>
        <td class="ui-helper-center"><%= part.quantity %></td>
        <td class="ui-helper-center"><%= "ห้อง" + Storage.find(part.storage_id).room + " โซน" + Storage.find(part.storage_id).zone %></td>
        <!-- <td><%= link_to 'นำเข้า', part %></td> -->
        <td class="ui-helper-center"><%= link_to 'เปลี่ยนจำนวน', edit_part_path(part) %></td>
        <td class="ui-helper-center"><%= link_to 'Destroy', part, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<style>
.p {
    word-spacing: 50px;
}
</style>

<%= link_to 'เพิ่มอะไหล่', new_part_path %>
